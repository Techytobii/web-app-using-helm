apiVersion: v2
name: myapp
description: A Helm chart for deploying my application

# Chart type: "application" or "library"
type: application

# Chart version — increment each time chart templates change
version: 0.1.0

# Application version — increment each time app changes
appVersion: "1.16.0"
